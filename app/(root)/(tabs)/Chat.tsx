import { SafeAreaView, View } from 'react-native';
import { Text } from 'react-native';
const Chat =()=>{
    return(
        <SafeAreaView>
            <Text>Home Screen</Text>
        </SafeAreaView>
    )
}

export default Chat;